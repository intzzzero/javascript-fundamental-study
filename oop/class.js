// https://developer.mozilla.org/ko/docs/Learn/JavaScript/Objects/Object-oriented_JS

/*
class Animal {
  constructor(name, sound) {
    this._name = name;
    this._sound = sound;
  }

  get name() {
    return this._name;
  }
  get sound() {
    return `Sound of ${this._name}: "${this._sound}"`;
  }
  set name(val) {
    this._name = val;
  }
  set sound(val) {
    this._sound = val;
  }
}

const cat = new Animal('Cat', 'Meowww');
console.log(cat.sound);

const dog = new Animal('Dog', 'Wangwangwang');
console.log(dog.sound);
*/

class Food {
  constructor() {
    
  }
}
